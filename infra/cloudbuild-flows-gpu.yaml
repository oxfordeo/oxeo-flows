steps:
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '.'
      - '--file=infra/Dockerfile.flows-gpu'
      - '--tag=eu.gcr.io/$PROJECT_ID/flows-gpu'
images:
  - 'eu.gcr.io/$PROJECT_ID/flows-gpu'
